import React from 'react';
import { Link } from 'react-router-dom';

function HistoricPubications() {
  /*// Suponiendo que tienes un estado o prop con las publicaciones
  const publications = [
    {
      id: 1,
      image: .selectedImage,
      description: 'Branca es una gata de color blanco con manchas grises. Se perdió por la zona del Colegio. Lleva puesto un collar rojo y chapa identificadora.'
      // ... otros datos que puedas necesitar
    },
    // ... más publicaciones
  ];

  return (
    <div style={{ padding: '20px' }}>
      <header style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <h1>My Posts</h1>
        <button>+</button>
      </header>
      {publications.map((publication) => (
        <div key={publication.id} style={{ border: '1px solid #ccc', borderRadius: '8px', padding: '10px', margin: '10px 0' }}>
          <img src={publication.image} alt="Publicación" style={{ width: '100%', height: 'auto', borderRadius: '8px' }} />
          <p>{publication.description}</p>
          <div style={{ display: 'flex', justifyContent: 'space-between' }}>
            <Link to={`/edit/${publication.id}`} className="button">Edit</Link>
            <button className="button">FInish</button>
          </div>
        </div>
      ))}
    </div>
  );*/
}

export default HistoricPubications;
